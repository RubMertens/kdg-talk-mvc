@model VotingApp.Controllers.QuestionViewModel

@if (Model.IsAnswered)
{
@*     @Html.DisplayForModel() *@
    <display for="@Model"></display>
}
else
{
@*     @Html.EditorForModel() *@
    <editor for="@Model"></editor>
}