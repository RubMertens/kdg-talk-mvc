@model Voting.WebApp.Controllers.QuestionViewModel

<div>
    @foreach (var pa in Model.Answers)
    {
        <button class="btn @(pa.IsSelectedAnswer ? "btn-primary" : "btn-outline-primary")" disabled>@pa.Answer</button>
    }
</div>